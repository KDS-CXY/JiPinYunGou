<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.baizhanshopping.shopping_admin_service.mapper.AdminMapper">
    <delete id="deleteAdminAllRole" parameterType="long">
        DELETE from bz_admin_role WHERE aid = #{aid}
    </delete>
    <resultMap id="AdminMapper" type="com.baizhanshopping.shopping_common.pojo.Admin">
        <id column="aid" property="aid"></id>
        <result property="username" column="username"></result>
        <collection property="roles" column="aid" ofType="com.baizhanshopping.shopping_common.pojo.Role">
            <id property="rid" column="rid"></id>
            <result property="roleName" column="roleName"></result>
            <result property="roleDesc" column="roleDesc"></result>
            <collection property="permissions" column="rid" ofType="com.baizhanshopping.shopping_common.pojo.Permission">
                <id property="pid" column="pid"></id>
                <result property="permissionName" column="permissionName"></result>
                <result property="url" column="url"></result>
            </collection>
        </collection>
    </resultMap>
    <select id="findByAid" parameterType="long" resultMap="AdminMapper">
        SELECT *
        FROM bz_admin
                 LEFT JOIN bz_admin_role
                           ON bz_admin.aid = bz_admin_role.aid
                 LEFT JOIN bz_role
                           ON bz_admin_role.rid = bz_role.rid
                 LEFT JOIN bz_role_permission
                           ON bz_role.rid = bz_role_permission.rid
                 LEFT JOIN bz_permission
                           ON bz_role_permission.pid = bz_permission.pid
        where bz_admin.aid=#{aid}
    </select>
    <insert id="addRoleToAdmin" parameterType="long">
        insert into bz_admin_role values (#{aid},#{rid})
    </insert>
</mapper>